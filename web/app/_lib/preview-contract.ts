import contract from "../../preview-contract.json";

export const PREVIEW_NEWS_PAGE_SIZE = contract.newsPageSize;
export const PREVIEW_RESOURCES = Object.freeze(contract.resources);
export const PREVIEW_STATUS_INLINE_KEYS = Object.freeze(contract.statusInlineKeys);
