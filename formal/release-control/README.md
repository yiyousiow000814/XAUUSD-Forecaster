# News migration formal verification

The retained model protects generation identity, visibility, activation and
cleanup. `shards.json` owns its safety properties and changed-source selection.
Custom release, CPU qualification, Access receipt renewal and watchdog models
are retired with their production owners. Historical audit stays in Git and the
existing decomposition audit; it does not select current publication behavior.

Real Windows runtime contracts protect the main owner and service lifecycle.
Worker CPU headroom and capacity tests protect real request budgets. Existing
Access/JWT and authentication tests protect private APIs. No human approval is
created by any of those tests.
