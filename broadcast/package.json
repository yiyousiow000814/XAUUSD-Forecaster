{
  "name": "aurum-live-broadcast",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "benchmark": "node tests/load-benchmark.mjs"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
